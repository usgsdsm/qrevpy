# -*- mode: python -*-

block_cipher = None


a = Analysis(['MainWidget.py'],
             pathex=['C:\\Users\\gpetrochenkov\\Documents\\Visual Studio 2017\\Projects\\TestFlask\\PythonApplication1\\Panels'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='MainWidget',
          debug=False,
          strip=False,
          upx=False,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=False,
               name='MainWidget')
